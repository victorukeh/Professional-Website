{
  "name": "personal-website",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "serve . -l ${PORT:-8080}",
    "dev": "node dev-server.mjs",
    "dev:serve": "serve . -l 8080"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
